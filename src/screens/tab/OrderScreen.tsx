import { View } from "react-native";
import PublicText from "../../components/common/PublicText";
import ScreenContainer from "../../components/layout/ScreenContainer";

const OrderScreen = () => {
    return (
        <ScreenContainer>
            <View>
                <PublicText>order</PublicText>
            </View>
        </ScreenContainer>
    );
};

export default OrderScreen;