import {FlatList} from 'react-native-gesture-handler';

const StoreInfoScreen = ({navigation, route}) => {
  const {storeId} = route.params;
  const data = [{
        title: '단팥빵',
    },{
        title: '단팥빵',
    }
  ]
  return (
    null
  );
};

export default StoreInfoScreen;
